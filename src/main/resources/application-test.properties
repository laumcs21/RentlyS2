# ============================================
# CONFIGURACIÓN PARA TESTS DE INTEGRACIÓN
# ============================================
# Esta configuración usa H2 en memoria para tests rápidos y aislados
# No afecta tu base de datos MySQL de producción

# 🔹 Base de datos H2 en memoria (se crea y destruye en cada test)
spring.datasource.url=jdbc:h2:mem:testdb;MODE=MySQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# 🔹 Hibernate/JPA para H2
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# 🔹 H2 Console deshabilitada en tests
spring.h2.console.enabled=false

# 🔹 Swagger deshabilitado en pruebas para mejor rendimiento
springdoc.swagger-ui.enabled=false
springdoc.api-docs.enabled=false

# 🔹 Configuración de logs (opcional, para debugging)
logging.level.org.springframework.test=INFO
logging.level.org.hibernate.SQL=INFO
logging.level.com.Rently=DEBUG

# 🔹 Deshabilitar banner de Spring en tests
spring.main.banner-mode=off
